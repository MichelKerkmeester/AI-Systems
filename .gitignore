# Git Ignore Configuration
# Specifies intentionally untracked files that Git should ignore

.DS_Store

# Development
.mcp.json
package-lock.json
package.json
postcss.config.js
tailwind.config.js
tsconfig.json
node_modules/
lib/

# IDE and editor folders (ignored at all levels)
.vscode/
.claude/

# Configuration files (ignored at all levels)
mcp_servers.json
settings.local.json

# Exception: Allow specific config files in Code Environment
!Code Environment/mcp.json
!Code Environment/settings.local.json

# Working directories (ignored at all levels)
context/
design system/
export/
memory/

# Archive folder (root level only)
/z — Archive/

# Complete folder ignore
Writer Systems/Barter/